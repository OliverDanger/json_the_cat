// const { fetcher } = require('../breedFetcher');
// const { assert } = require('chai');

// describe('Fetcher - fetches page info', () => {
//   it(`returns an object with url and other info via callback`, (done) => {
//     const data = fetcher(`https://api.thecatapi.com/v1/images/search?breed_id=asdf`) 
//     assert.equal(data, `No cat breed by that name!`)

//     done();
//   })
// })

'I used promises and already tested through the promise structure'